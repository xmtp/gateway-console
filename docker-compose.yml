version: '3.8'

services:
  gateway:
    image: xmtp/xmtpd-gateway:main
    ports:
      - "5050:5050"
      - "5055:5055"
    environment:
      - XMTPD_PAYER_PRIVATE_KEY=${PAYER_PRIVATE_KEY}
    restart: unless-stopped
